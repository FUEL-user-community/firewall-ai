"""
PAN-OS Modular Toolkit — Firewall diagnostic tools.

Modules:
    client.py       — PanOSClient (unified API connection pool)
    ops.py          — Operational commands, policy simulation
    config.py       — Configuration reads, User-ID audits
    logs.py         — Log queries, reports, analytics
    research.py     — External documentation search (Google CSE)
    metrics.py      — Conversion metrics (console logging)
    interceptors.py — Command routing (CLI → handler dispatch)
"""
